
// This file doesn't actually compile anything useful, it just checks that
// we can properly specify the arch strings for Scalar crypto on
// the command line.

int main(int argc, char ** argv) {
    return 0;
}
